import 'package:flutter/material.dart';

class SelectPagoScreen extends StatelessWidget {
  const SelectPagoScreen({Key? key, required this.data}) : super(key: key);

  final Object data;

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
